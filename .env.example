# Copy to `.env` (repo root, gitignored) and fill in.
# Read by llm.backend.OpenAIBackend; never overrides a variable already in the
# real environment.

OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
